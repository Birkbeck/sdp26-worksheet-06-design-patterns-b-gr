package prototype;

public class AccessControlProvider {
    public static AccessControl getAccessControlObject(String controlLevel) {
        // TODO: use prototype access control objects (return clones based on the control level)
        //return switch (controlLevel) {
            //case "USER" -> new AccessControl().setAccess("userAccessControl");
            //case "MANAGER" -> new AccessControl().setAccess("managerAccessControl");
        //    default -> throw new IllegalStateException("Unexpected value: " + controlLevel);
        //};
        return null;
    }
}
